# pnpm 11+ 只读 .npmrc（不读环境变量）。
# registry 选择随网络实测动态调整：
#   2026-09-16 前：npmjs 通、npmmirror metadata ECONNRESET → 主源 npmjs。
#   2026-09-16（Docker Desktop 代理开启后）：Docker 构建/容器内 npmjs 持续
#   ECONNRESET、npmmirror 200 可达 → 主源切换为 npmmirror（宿主/构建内均验证）。
registry=https://registry.npmmirror.com
# pnpm 11 verify-deps-before-run 会在 pnpm dsh web 前自动 install（npmmirror 故障时卡死启动）；
# 镜像内 node_modules 由构建期 install 保证完整，运行期禁止再触发 install。
verify-deps-before-run=false
