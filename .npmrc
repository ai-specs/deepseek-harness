# pnpm 11+ 只读 .npmrc（不读环境变量）。
# npm 官方源完整且当前网络实测可用；npmmirror 当前 metadata 端点整体 ECONNRESET，仅作回退。
registry=https://registry.npmjs.org
# pnpm 11 verify-deps-before-run 会在 pnpm dsh web 前自动 install（npmmirror 故障时卡死启动）；
# 镜像内 node_modules 由构建期 install 保证完整，运行期禁止再触发 install。
verify-deps-before-run=false
